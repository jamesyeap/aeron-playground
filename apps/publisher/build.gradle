plugins {
    id 'com.gradleup.shadow'
}

apply from: "${rootDir}/gradle/sbe.gradle"

// Fat jar: build/libs/publisher-<version>-all.jar
tasks.named('shadowJar') {
    manifest {
        attributes 'Main-Class': 'publisher.Publisher'
    }
}
